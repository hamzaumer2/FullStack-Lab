import * as React from 'react';
import {Button} from '@mui/material';
import './button.css';

export default function BasicButtons() {
  return (
    <>
      <Button className="color" variant="contained">Contained</Button>
    </>
  );
}